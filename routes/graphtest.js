var FB = require('fb');
FB.setAccessToken('');
module.exports = function (app, passport) {
/*FB.api('https://www.facebook.com/app_scoped_user_id/10152667117269170/', function (res) {
    if(!res || res.error) {
        console.log(!res ? 'error occurred' : res.error);
        return;
    }
    console.log(res);
    console.log(res.name);
});*/
}